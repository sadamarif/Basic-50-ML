# Load library
import numpy as np
# Set seed
np.random.seed(0)
# Generate five random floats between 0.0 and 1.0
np.random.random(5)

# Generate five random integers between 1 and 10
np.random.randint(0, 11, 5)

# Draw five numbers from a normal distribution with mean 0.0
# and standard deviation of 1.0
np.random.normal(0.0, 1.0, 5)